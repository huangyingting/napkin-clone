# Runtime Configuration Inventory

**Status:** Current  
**Last updated:** 2026-06-25

This is the single inventory for environment variables read by the app server,
client bundle, plain Node scripts, Prisma tooling, and E2E tooling. Source code,
tests, and schemas remain authoritative.

`NEXT_PUBLIC_*` values are statically inlined by Next.js into browser bundles at
build time. The helpers in `src/lib/client-config.ts` intentionally keep literal
`process.env.NEXT_PUBLIC_*` reads; do not replace them with computed keys or a
generic accessor. Server-only env access stays in `src/lib/env.ts` and the owned
runtime config modules listed below.

| Name                                | Context                               | Default                                                                                                                       | Required / optional                                                                           | Production behavior                                                                                                                                                            | Owner                            |
| ----------------------------------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------- |
| `NODE_ENV`                          | App server / scripts                  | Unset is treated as non-production; `npm start` sets `production`                                                             | Optional                                                                                      | Enables production-only secure-cookie behavior, disables dev Prisma global caching, suppresses dev email-link logging, and makes mock billing fail closed for paid-plan grants | Runtime platform                 |
| `PORT`                              | App server / inline collab script     | `4000`                                                                                                                        | Optional                                                                                      | App HTTP server and inline `/collab` websocket bind to this port                                                                                                               | Runtime platform / collaboration |
| `HOST`                              | App server / inline collab script     | `0.0.0.0`                                                                                                                     | Optional                                                                                      | App server bind address; keep behind the deployment proxy as appropriate                                                                                                       | Runtime platform                 |
| `COLLAB_INLINE`                     | App server script                     | Enabled unless exactly `0`                                                                                                    | Optional                                                                                      | Inline `/collab` websocket is on by default; set `0` when using only the standalone collab server                                                                              | Collaboration                    |
| `DB_PROVIDER`                       | App server / Prisma CLI / scripts     | `sqlite` unless exactly `postgres`                                                                                            | Optional                                                                                      | Set `postgres` for production Postgres; any other value selects SQLite                                                                                                         | Database                         |
| `DATABASE_URL`                      | App server / Prisma CLI / scripts     | SQLite: `file:./prisma/dev.db`; Postgres: none                                                                                | Required when `DB_PROVIDER=postgres`; optional for SQLite                                     | Production Postgres must provide a connection string; SQLite dev/test can omit it                                                                                              | Database                         |
| `AUTH_SECRET`                       | App server                            | None                                                                                                                          | Required for production; some routes return a safe 500 when absent                            | Auth sessions and signed anonymous generation cookies require it; missing value is treated as server misconfiguration                                                          | Auth / generation security       |
| `AUTH_URL`                          | Auth.js / standalone collab script    | Collab standalone falls back to `http://127.0.0.1:4000`                                                                       | Optional but recommended in deployed environments                                             | Standalone collab uses it to derive `/api/collab/authorize` and `/api/collab/flush` unless `COLLAB_AUTHORIZE_URL` overrides authorization                                      | Auth / collaboration             |
| `NEXT_PUBLIC_APP_URL`               | Client bundle / server helpers        | Public helper: `http://localhost:4000`; Stripe checkout caller fallback: `http://localhost:3000`                              | Optional                                                                                      | Must be set before building the client bundle for deployed links; server helpers use current env at call time                                                                  | App URLs / billing redirects     |
| `GOOGLE_CLIENT_ID`                  | App server                            | None                                                                                                                          | Optional; required with `GOOGLE_CLIENT_SECRET` to enable Google sign-in                       | Google provider is not registered unless both values are present                                                                                                               | Auth                             |
| `GOOGLE_CLIENT_SECRET`              | App server                            | None                                                                                                                          | Optional; required with `GOOGLE_CLIENT_ID` to enable Google sign-in                           | Google provider is not registered unless both values are present                                                                                                               | Auth                             |
| `AZURE_OPENAI_ENDPOINT`             | App server                            | None                                                                                                                          | Optional until generation is used; required with `AZURE_OPENAI_API_KEY` for Azure generation  | Azure-backed generation fails configuration checks unless endpoint and key are set                                                                                             | AI generation                    |
| `AZURE_OPENAI_API_KEY`              | App server                            | None                                                                                                                          | Optional until generation is used; required with `AZURE_OPENAI_ENDPOINT` for Azure generation | Azure-backed generation fails configuration checks unless endpoint and key are set                                                                                             | AI generation                    |
| `AZURE_OPENAI_DEPLOYMENT`           | App server                            | `gpt-5.5`                                                                                                                     | Optional                                                                                      | Selects the Azure OpenAI deployment name used by generation routes                                                                                                             | AI generation                    |
| `AZURE_OPENAI_API_VERSION`          | App server                            | `2024-10-21`                                                                                                                  | Optional                                                                                      | Selects the Azure OpenAI REST api-version                                                                                                                                      | AI generation                    |
| `AI_DECK_GEN_ENABLED`               | App server                            | Disabled (`false`)                                                                                                            | Optional opt-in                                                                               | `POST /api/generate-deck` returns 404 unless explicitly truthy (`1`, `true`, `yes`, `on`)                                                                                      | AI deck generation               |
| `NEXT_PUBLIC_AI_DECK_GEN_ENABLED`   | Client bundle                         | Disabled (`false`)                                                                                                            | Optional opt-in                                                                               | Shows the client deck-generation entry point only when built with a truthy value; server flag remains the security boundary                                                    | AI deck generation               |
| `ANON_GENERATION_LIMIT`             | App server                            | `5`                                                                                                                           | Optional                                                                                      | Positive integer only; invalid, zero, negative, or missing values fall back to the default                                                                                     | AI generation quota              |
| `USER_GENERATION_RATE_LIMIT`        | App server                            | `30`                                                                                                                          | Optional                                                                                      | Positive integer only; limits authenticated generations per window                                                                                                             | AI generation quota              |
| `USER_GENERATION_RATE_WINDOW_MS`    | App server                            | `60000`                                                                                                                       | Optional                                                                                      | Positive integer only; generation rate-limit window in milliseconds                                                                                                            | AI generation quota              |
| `ANON_IP_GENERATION_RATE_LIMIT`     | App server                            | `20`                                                                                                                          | Optional                                                                                      | Positive integer only; server-side anonymous per-IP generation throttle                                                                                                        | API abuse controls               |
| `ANON_IP_GENERATION_RATE_WINDOW_MS` | App server                            | `3600000`                                                                                                                     | Optional                                                                                      | Positive integer only; anonymous per-IP generation window in milliseconds                                                                                                      | API abuse controls               |
| `IMPORT_RATE_LIMIT`                 | App server                            | `10`                                                                                                                          | Optional                                                                                      | Positive integer only; import route per-IP request limit                                                                                                                       | API abuse controls               |
| `IMPORT_RATE_WINDOW_MS`             | App server                            | `60000`                                                                                                                       | Optional                                                                                      | Positive integer only; import rate-limit window in milliseconds                                                                                                                | API abuse controls               |
| `BILLING_UNLIMITED_CREDITS`         | App server                            | Disabled (`false`)                                                                                                            | Optional opt-in                                                                               | Credits remain metered unless explicitly truthy; enabling bypasses metering for authenticated users                                                                            | Billing / metering               |
| `STRIPE_SECRET_KEY`                 | App server                            | None                                                                                                                          | Optional in non-production; required for production billing provider mode                     | Selects Stripe billing when present. In production, absence makes billing provider selection fail closed instead of using mock paid-plan grants                                | Billing                          |
| `STRIPE_PLUS_PRICE_ID`              | App server                            | None                                                                                                                          | Required for Stripe Plus checkout and price mapping                                           | Missing value fails when Plus checkout needs a Stripe price id                                                                                                                 | Billing                          |
| `STRIPE_PRO_PRICE_ID`               | App server                            | None                                                                                                                          | Required for Stripe Pro checkout and price mapping                                            | Missing value fails when Pro checkout needs a Stripe price id                                                                                                                  | Billing                          |
| `STRIPE_WEBHOOK_SECRET`             | App server                            | None                                                                                                                          | Required for Stripe webhook verification when Stripe is configured                            | Webhook handler returns 500 if Stripe is configured but the signing secret is missing                                                                                          | Billing                          |
| `COLLAB_SINGLE_INSTANCE`            | App server / standalone collab script | Unset                                                                                                                         | Optional but recommended for single-instance production                                       | `1` or `true` declares single-instance mode and suppresses startup advisory warnings                                                                                           | Collaboration deployment         |
| `COLLAB_INSTANCE_COUNT`             | App server / standalone collab script | `1`                                                                                                                           | Optional                                                                                      | Values greater than 1 require `COLLAB_STICKY_ROUTING=1`; otherwise startup is unhealthy and exits                                                                              | Collaboration deployment         |
| `COLLAB_STICKY_ROUTING`             | App server / standalone collab script | Unset                                                                                                                         | Required only when `COLLAB_INSTANCE_COUNT>1`                                                  | `1` or `true` declares sticky routing and allows multi-instance collab startup                                                                                                 | Collaboration deployment         |
| `COLLAB_PORT`                       | Standalone collab script              | `1234`                                                                                                                        | Optional                                                                                      | Standalone websocket and `/health` server listen port                                                                                                                          | Collaboration standalone server  |
| `COLLAB_HOST`                       | Standalone collab script              | `0.0.0.0`                                                                                                                     | Optional                                                                                      | Standalone collab bind address                                                                                                                                                 | Collaboration standalone server  |
| `COLLAB_AUTHORIZE_URL`              | Standalone collab script              | `${AUTH_URL}/api/collab/authorize`, or `http://127.0.0.1:4000/api/collab/authorize`                                           | Optional                                                                                      | Overrides the app authorization endpoint used before accepting standalone websocket upgrades                                                                                   | Collaboration auth               |
| `COLLAB_INTERNAL_SECRET`            | App server / standalone collab script | None                                                                                                                          | Optional; required to enable eviction recovery snapshots                                      | Flush endpoint compares this shared secret in constant time; when unset, endpoint returns 503 and script flusher is a no-op with a warning                                     | Collaboration recovery security  |
| `NEXT_PUBLIC_COLLAB_WS_URL`         | Client bundle                         | Browser: derive from page origin + `/collab`; SSR fallback uses `ws://localhost:${NEXT_PUBLIC_COLLAB_WS_PORT ?? 4000}/collab` | Optional                                                                                      | Set before build to point clients at a standalone `wss://` endpoint; explicit value always wins                                                                                | Collaboration client             |
| `NEXT_PUBLIC_COLLAB_WS_PORT`        | Client bundle                         | `4000`                                                                                                                        | Optional                                                                                      | Only affects SSR/non-browser websocket fallback when `NEXT_PUBLIC_COLLAB_WS_URL` is unset                                                                                      | Collaboration client             |
| `I18N_SWITCHER_ENABLED`             | App server                            | Disabled (`false`) unless exactly `true`                                                                                      | Optional opt-in                                                                               | Language switcher stays hidden unless exactly `true`; partial catalog infrastructure remains available                                                                         | Internationalization             |
| `E2E_BASE_URL`                      | Playwright config                     | `http://localhost:3000` after `BASE_URL` fallback                                                                             | Optional                                                                                      | Highest-priority app base URL for E2E runs                                                                                                                                     | E2E tooling                      |
| `BASE_URL`                          | Playwright config                     | `http://localhost:3000`                                                                                                       | Optional                                                                                      | Fallback app base URL when `E2E_BASE_URL` is unset                                                                                                                             | E2E tooling                      |
| `E2E_WEB_SERVER`                    | Playwright config                     | Disabled unless exactly `1`                                                                                                   | Optional                                                                                      | `1` lets Playwright start `npm run dev`; otherwise tests expect an existing server                                                                                             | E2E tooling                      |
| `CI`                                | Playwright config / CI environment    | Unset / false                                                                                                                 | Optional                                                                                      | Enables forbid-only, GitHub reporter, retries, one worker, and disables web-server reuse                                                                                       | E2E tooling / CI                 |
| `E2E_PROFILE`                       | E2E specs                             | Disabled unless exactly `1`                                                                                                   | Optional                                                                                      | `1` enables deterministic profile-only specs after profile seed data exists                                                                                                    | E2E tooling                      |

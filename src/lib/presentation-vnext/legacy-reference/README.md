# Legacy presentation logic reference

This directory is a migration-only snapshot of `src/lib/presentation`.
TypeScript declaration and implementation files are copied with a `.legacy`
suffix so they remain searchable/editable reference material without becoming
active vNext code.

Use these files to port stage math, commands, selection, autosave, and helper
behavior into v7-native modules. Do not import from this directory in product
code.
